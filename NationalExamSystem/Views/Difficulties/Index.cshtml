@model IEnumerable<NationalExamSystem.Models.Difficulty>

@{
    ViewBag.Title = "Index";
    Layout = "../Admin/layout.cshtml";

}

<br />
<br />
<br />
<br />
<br />
<br />



<table class="table table-striped table-advance table-hover" style="margin-left:12%">
   
    <tr>
        <td>
            <a href="@Url.Action("Create")" class="btn btn-info">Add New Difficulty</a>
        </td>
    </tr>

     <tr>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Level)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
